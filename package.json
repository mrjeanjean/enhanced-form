{
  "name": "enhanced-form-monorepo",
  "private": true,
  "scripts": {
    "dev-core": "yarn --cwd enhanced-form-core/ run build --watch",
    "dev-file-manager": "yarn --cwd file-upload-manager/ run build --watch",
    "dev-examples": "yarn --cwd enhanced-form-examples/ run dev",
    "dev-admin": "yarn --cwd enhanced-form-admin/ run dev"
  },
  "workspaces": ["enhanced-form-examples", "enhanced-form-core", "file-upload-manager"]
}
